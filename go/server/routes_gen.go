// Code generated by routegen. DO NOT EDIT.

package server

import (
	"context"
	"net/http"

	publicv1 "github.com/metacensus/api/go/metacensus/public/v1"
	v1 "github.com/metacensus/api/go/metacensus/v1"
)

// AuthRoutes is what an implementation of the AuthRoutes service provides.
// One method per route; the request carries path, query and body fields
// already bound. Its routes hang off routes.Prefix.
type AuthRoutes interface {
	// POST /login
	Login(context.Context, *v1.LoginRequest) (*v1.Session, error)
	// POST /signup
	SignUp(context.Context, *v1.SignUpRequest) (*v1.Session, error)
	// POST /logout
	Logout(context.Context, *v1.LogoutRequest) (*v1.LogoutResponse, error)
}

// UnimplementedAuthRoutes answers every AuthRoutes route with 501. Embed it to
// implement a service one route at a time.
type UnimplementedAuthRoutes struct{}

func (UnimplementedAuthRoutes) Login(context.Context, *v1.LoginRequest) (*v1.Session, error) {
	return nil, errNotImplemented("AuthRoutes.Login")
}

func (UnimplementedAuthRoutes) SignUp(context.Context, *v1.SignUpRequest) (*v1.Session, error) {
	return nil, errNotImplemented("AuthRoutes.SignUp")
}

func (UnimplementedAuthRoutes) Logout(context.Context, *v1.LogoutRequest) (*v1.LogoutResponse, error) {
	return nil, errNotImplemented("AuthRoutes.Logout")
}

// RegisterAuthRoutes registers every AuthRoutes route on mux, under rt.Prefix,
// which for this service is routes.Prefix.
//
// How to mount: see [Mux], [Runtime.Prefix] and [Runtime.PathValue].
func RegisterAuthRoutes(mux Mux, rt *Runtime, impl AuthRoutes) {
	rt.checkPathValue(mux)
	mux.Method("POST", rt.Prefix+"/login", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.LoginRequest)
		raw, err := rt.readBody(w, r)
		if err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.decodeBody(raw, req); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.Login(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("POST", rt.Prefix+"/signup", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.SignUpRequest)
		raw, err := rt.readBody(w, r)
		if err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.decodeBody(raw, req); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		// Signed route: the two halves must be present, and every id the
		// path bound must match its signed copy. Neither check looks at the
		// signature itself — that is persistence's, inside the chaincode
		// boundary. See requireField and contentParam.
		if err := requireField(req.Content != nil, "content"); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := requireField(req.UserSignature != nil, "userSignature"); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.SignUp(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("POST", rt.Prefix+"/logout", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.LogoutRequest)
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.Logout(r.Context(), req)
		rt.respond(w, resp, err)
	}))
}

// HealthRoutes is what an implementation of the HealthRoutes service provides.
// One method per route; the request carries path, query and body fields
// already bound. Its routes hang off routes.Prefix.
type HealthRoutes interface {
	// GET /healthcheck
	Healthcheck(context.Context, *v1.HealthcheckRequest) (*v1.HealthcheckResponse, error)
}

// UnimplementedHealthRoutes answers every HealthRoutes route with 501. Embed it to
// implement a service one route at a time.
type UnimplementedHealthRoutes struct{}

func (UnimplementedHealthRoutes) Healthcheck(context.Context, *v1.HealthcheckRequest) (*v1.HealthcheckResponse, error) {
	return nil, errNotImplemented("HealthRoutes.Healthcheck")
}

// RegisterHealthRoutes registers every HealthRoutes route on mux, under rt.Prefix,
// which for this service is routes.Prefix.
//
// How to mount: see [Mux], [Runtime.Prefix] and [Runtime.PathValue].
func RegisterHealthRoutes(mux Mux, rt *Runtime, impl HealthRoutes) {
	rt.checkPathValue(mux)
	mux.Method("GET", rt.Prefix+"/healthcheck", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.HealthcheckRequest)
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.Healthcheck(r.Context(), req)
		rt.respond(w, resp, err)
	}))
}

// PropRoutes is what an implementation of the PropRoutes service provides.
// One method per route; the request carries path, query and body fields
// already bound. Its routes hang off routes.Prefix.
type PropRoutes interface {
	// GET /topic/{topicId}/prop
	ListProps(context.Context, *v1.PropListRequest) (*v1.PropList, error)
	// GET /topic/{topicId}/prop/{propId}
	GetProp(context.Context, *v1.PropGetRequest) (*v1.Prop, error)
	// POST /topic/{topicId}/prop
	CreateProp(context.Context, *v1.PropCreateRequest) (*v1.Prop, error)
	// GET /topic/{topicId}/prop/{propId}/vote
	ListVotes(context.Context, *v1.VoteListRequest) (*v1.VoteList, error)
	// POST /topic/{topicId}/prop/{propId}/vote
	SetVote(context.Context, *v1.VoteSetRequest) (*v1.Vote, error)
}

// UnimplementedPropRoutes answers every PropRoutes route with 501. Embed it to
// implement a service one route at a time.
type UnimplementedPropRoutes struct{}

func (UnimplementedPropRoutes) ListProps(context.Context, *v1.PropListRequest) (*v1.PropList, error) {
	return nil, errNotImplemented("PropRoutes.ListProps")
}

func (UnimplementedPropRoutes) GetProp(context.Context, *v1.PropGetRequest) (*v1.Prop, error) {
	return nil, errNotImplemented("PropRoutes.GetProp")
}

func (UnimplementedPropRoutes) CreateProp(context.Context, *v1.PropCreateRequest) (*v1.Prop, error) {
	return nil, errNotImplemented("PropRoutes.CreateProp")
}

func (UnimplementedPropRoutes) ListVotes(context.Context, *v1.VoteListRequest) (*v1.VoteList, error) {
	return nil, errNotImplemented("PropRoutes.ListVotes")
}

func (UnimplementedPropRoutes) SetVote(context.Context, *v1.VoteSetRequest) (*v1.Vote, error) {
	return nil, errNotImplemented("PropRoutes.SetVote")
}

// RegisterPropRoutes registers every PropRoutes route on mux, under rt.Prefix,
// which for this service is routes.Prefix.
//
// How to mount: see [Mux], [Runtime.Prefix] and [Runtime.PathValue].
func RegisterPropRoutes(mux Mux, rt *Runtime, impl PropRoutes) {
	rt.checkPathValue(mux)
	mux.Method("GET", rt.Prefix+"/topic/{topicId}/prop", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.PropListRequest)
		var err error
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.TopicId, err = rt.pathParam(r, "topicId", req.TopicId); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.ListProps(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("GET", rt.Prefix+"/topic/{topicId}/prop/{propId}", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.PropGetRequest)
		var err error
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.TopicId, err = rt.pathParam(r, "topicId", req.TopicId); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.PropId, err = rt.pathParam(r, "propId", req.PropId); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.GetProp(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("POST", rt.Prefix+"/topic/{topicId}/prop", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.PropCreateRequest)
		raw, err := rt.readBody(w, r)
		if err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.decodeBody(raw, req); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.TopicId, err = rt.pathParam(r, "topicId", req.TopicId); err != nil {
			rt.writeError(w, err)
			return
		}
		// Signed route: the two halves must be present, and every id the
		// path bound must match its signed copy. Neither check looks at the
		// signature itself — that is persistence's, inside the chaincode
		// boundary. See requireField and contentParam.
		if err := requireField(req.Content != nil, "content"); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := requireField(req.UserSignature != nil, "userSignature"); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := contentParam("topicId", req.TopicId, req.Content.TopicId); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.CreateProp(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("GET", rt.Prefix+"/topic/{topicId}/prop/{propId}/vote", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.VoteListRequest)
		var err error
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.TopicId, err = rt.pathParam(r, "topicId", req.TopicId); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.PropId, err = rt.pathParam(r, "propId", req.PropId); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.ListVotes(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("POST", rt.Prefix+"/topic/{topicId}/prop/{propId}/vote", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.VoteSetRequest)
		raw, err := rt.readBody(w, r)
		if err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.decodeBody(raw, req); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.TopicId, err = rt.pathParam(r, "topicId", req.TopicId); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.PropId, err = rt.pathParam(r, "propId", req.PropId); err != nil {
			rt.writeError(w, err)
			return
		}
		// Signed route: the two halves must be present, and every id the
		// path bound must match its signed copy. Neither check looks at the
		// signature itself — that is persistence's, inside the chaincode
		// boundary. See requireField and contentParam.
		if err := requireField(req.Content != nil, "content"); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := requireField(req.UserSignature != nil, "userSignature"); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := contentParam("topicId", req.TopicId, req.Content.TopicId); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := contentParam("propId", req.PropId, req.Content.PropId); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.SetVote(r.Context(), req)
		rt.respond(w, resp, err)
	}))
}

// TopicRoutes is what an implementation of the TopicRoutes service provides.
// One method per route; the request carries path, query and body fields
// already bound. Its routes hang off routes.Prefix.
type TopicRoutes interface {
	// GET /topic
	ListTopics(context.Context, *v1.TopicListRequest) (*v1.TopicList, error)
	// GET /topic/{topicId}
	GetTopic(context.Context, *v1.TopicGetRequest) (*v1.Topic, error)
	// POST /topic
	CreateTopic(context.Context, *v1.TopicCreateRequest) (*v1.Topic, error)
	// GET /topic/{topicId}/member
	ListMembers(context.Context, *v1.MemberListRequest) (*v1.MemberList, error)
	// GET /topic/{topicId}/member/{userId}
	GetMember(context.Context, *v1.MemberGetRequest) (*v1.Member, error)
}

// UnimplementedTopicRoutes answers every TopicRoutes route with 501. Embed it to
// implement a service one route at a time.
type UnimplementedTopicRoutes struct{}

func (UnimplementedTopicRoutes) ListTopics(context.Context, *v1.TopicListRequest) (*v1.TopicList, error) {
	return nil, errNotImplemented("TopicRoutes.ListTopics")
}

func (UnimplementedTopicRoutes) GetTopic(context.Context, *v1.TopicGetRequest) (*v1.Topic, error) {
	return nil, errNotImplemented("TopicRoutes.GetTopic")
}

func (UnimplementedTopicRoutes) CreateTopic(context.Context, *v1.TopicCreateRequest) (*v1.Topic, error) {
	return nil, errNotImplemented("TopicRoutes.CreateTopic")
}

func (UnimplementedTopicRoutes) ListMembers(context.Context, *v1.MemberListRequest) (*v1.MemberList, error) {
	return nil, errNotImplemented("TopicRoutes.ListMembers")
}

func (UnimplementedTopicRoutes) GetMember(context.Context, *v1.MemberGetRequest) (*v1.Member, error) {
	return nil, errNotImplemented("TopicRoutes.GetMember")
}

// RegisterTopicRoutes registers every TopicRoutes route on mux, under rt.Prefix,
// which for this service is routes.Prefix.
//
// How to mount: see [Mux], [Runtime.Prefix] and [Runtime.PathValue].
func RegisterTopicRoutes(mux Mux, rt *Runtime, impl TopicRoutes) {
	rt.checkPathValue(mux)
	mux.Method("GET", rt.Prefix+"/topic", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.TopicListRequest)
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.ListTopics(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("GET", rt.Prefix+"/topic/{topicId}", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.TopicGetRequest)
		var err error
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.TopicId, err = rt.pathParam(r, "topicId", req.TopicId); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.GetTopic(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("POST", rt.Prefix+"/topic", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.TopicCreateRequest)
		raw, err := rt.readBody(w, r)
		if err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.decodeBody(raw, req); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		// Signed route: the two halves must be present, and every id the
		// path bound must match its signed copy. Neither check looks at the
		// signature itself — that is persistence's, inside the chaincode
		// boundary. See requireField and contentParam.
		if err := requireField(req.Content != nil, "content"); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := requireField(req.UserSignature != nil, "userSignature"); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.CreateTopic(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("GET", rt.Prefix+"/topic/{topicId}/member", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.MemberListRequest)
		var err error
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.TopicId, err = rt.pathParam(r, "topicId", req.TopicId); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.ListMembers(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("GET", rt.Prefix+"/topic/{topicId}/member/{userId}", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.MemberGetRequest)
		var err error
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.TopicId, err = rt.pathParam(r, "topicId", req.TopicId); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.UserId, err = rt.pathParam(r, "userId", req.UserId); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.GetMember(r.Context(), req)
		rt.respond(w, resp, err)
	}))
}

// UserRoutes is what an implementation of the UserRoutes service provides.
// One method per route; the request carries path, query and body fields
// already bound. Its routes hang off routes.Prefix.
type UserRoutes interface {
	// GET /user
	ListUsers(context.Context, *v1.UserListRequest) (*v1.UserList, error)
	// GET /user/{userId}
	GetUser(context.Context, *v1.UserGetRequest) (*v1.User, error)
	// GET /self
	GetSelf(context.Context, *v1.SelfGetRequest) (*v1.User, error)
}

// UnimplementedUserRoutes answers every UserRoutes route with 501. Embed it to
// implement a service one route at a time.
type UnimplementedUserRoutes struct{}

func (UnimplementedUserRoutes) ListUsers(context.Context, *v1.UserListRequest) (*v1.UserList, error) {
	return nil, errNotImplemented("UserRoutes.ListUsers")
}

func (UnimplementedUserRoutes) GetUser(context.Context, *v1.UserGetRequest) (*v1.User, error) {
	return nil, errNotImplemented("UserRoutes.GetUser")
}

func (UnimplementedUserRoutes) GetSelf(context.Context, *v1.SelfGetRequest) (*v1.User, error) {
	return nil, errNotImplemented("UserRoutes.GetSelf")
}

// RegisterUserRoutes registers every UserRoutes route on mux, under rt.Prefix,
// which for this service is routes.Prefix.
//
// How to mount: see [Mux], [Runtime.Prefix] and [Runtime.PathValue].
func RegisterUserRoutes(mux Mux, rt *Runtime, impl UserRoutes) {
	rt.checkPathValue(mux)
	mux.Method("GET", rt.Prefix+"/user", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.UserListRequest)
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.ListUsers(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("GET", rt.Prefix+"/user/{userId}", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.UserGetRequest)
		var err error
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		if req.UserId, err = rt.pathParam(r, "userId", req.UserId); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.GetUser(r.Context(), req)
		rt.respond(w, resp, err)
	}))
	mux.Method("GET", rt.Prefix+"/self", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(v1.SelfGetRequest)
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.GetSelf(r.Context(), req)
		rt.respond(w, resp, err)
	}))
}

// PartnerRoutes is what an implementation of the PartnerRoutes service provides.
// One method per route; the request carries path, query and body fields
// already bound. Its routes hang off routes.PublicPrefix.
type PartnerRoutes interface {
	// POST /partner
	SubmitPartnerInterest(context.Context, *publicv1.PartnerSubmission) (*publicv1.PartnerReceipt, error)
}

// UnimplementedPartnerRoutes answers every PartnerRoutes route with 501. Embed it to
// implement a service one route at a time.
type UnimplementedPartnerRoutes struct{}

func (UnimplementedPartnerRoutes) SubmitPartnerInterest(context.Context, *publicv1.PartnerSubmission) (*publicv1.PartnerReceipt, error) {
	return nil, errNotImplemented("PartnerRoutes.SubmitPartnerInterest")
}

// RegisterPartnerRoutes registers every PartnerRoutes route on mux, under rt.Prefix,
// which for this service is routes.PublicPrefix.
//
// How to mount: see [Mux], [Runtime.Prefix] and [Runtime.PathValue].
func RegisterPartnerRoutes(mux Mux, rt *Runtime, impl PartnerRoutes) {
	rt.checkPathValue(mux)
	mux.Method("POST", rt.Prefix+"/partner", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		req := new(publicv1.PartnerSubmission)
		raw, err := rt.readBody(w, r)
		if err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.decodeBody(raw, req); err != nil {
			rt.writeError(w, err)
			return
		}
		if err := rt.bindQuery(r, req, nil); err != nil {
			rt.writeError(w, err)
			return
		}
		resp, err := impl.SubmitPartnerInterest(r.Context(), req)
		rt.respond(w, resp, err)
	}))
}
