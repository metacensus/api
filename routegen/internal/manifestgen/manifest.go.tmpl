{{define "header"}}// Code generated by routegen. DO NOT EDIT.

// Package routes is the whole route table of {{.APIPrefix}} on one
// screen, generated from the google.api.http annotations on each resource's
// service in metacensus/v1.
package routes

// Prefix is the path every route below hangs off. Join it with a Route's
// Path to get the path a client actually requests; the reverse proxy in
// front of the API is what makes that resolve.
const Prefix = {{printf "%q" .APIPrefix}}

// Route is one declared route. Path is relative to {{.APIPrefix}} and
// spells its parameters {lowerCamelCase}, as the wire does. Params, Query and
// Body between them account for every field of Request: Params bind path
// segments, Query travels in the query string, and Body is "*" when the rest
// travels in the body and empty when none does.
type Route struct {
	Service  string
	RPC      string
	Method   string
	Path     string
	Params   []string
	Query    []string
	Body     string
	Request  string
	Response string
}

// Routes is every route across every service, ordered by file then by
// declaration.
var Routes = []Route{
{{end}}
{{define "route"}}	{Service: {{printf "%q" .Service}}, RPC: {{printf "%q" .RPC}}, Method: {{printf "%q" .Method}}, Path: {{printf "%q" .Path}}, Params: {{goSlice .Params}}, Query: {{goSlice .Query}}, Body: {{printf "%q" .Body}}, Request: {{printf "%q" .Request}}, Response: {{printf "%q" .Response}}},
{{end}}
{{define "footer"}}}
{{end}}
