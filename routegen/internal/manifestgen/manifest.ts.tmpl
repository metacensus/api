{{define "header"}}// Code generated by routegen. DO NOT EDIT.

// Every MetaCensus route on one screen, generated from the google.api.http
// annotations on each service. One manifest for both surfaces; see the
// repository README, "Routes". Each surface has its own prefix:
//
{{range .Packages}}//   {{printf "%-12s %-20s %s" .TSConst .Prefix .Summary}}
{{end}}{{end}}
{{define "prefix"}}
// The path {{.Proto}} routes hang off ({{.Summary}}). Join it with a route's
// `path` for the path a client requests; a route carries its own `prefix`, so
// joining needs no knowledge of which surface it came from.
export const {{.TSConst}} = {{printf "%q" .Prefix}};
{{end}}
{{define "routeType"}}
// `path` is relative to `prefix` and spells its parameters {lowerCamelCase}.
// `params`, `query` and `body` between them account for every field of
// `request`. `signed` means the request carries content and a userSignature over
// it, so the caller needs a signing key; see the repository README, "Routes".
export interface Route {
  readonly prefix: string;
  readonly service: string;
  readonly rpc: string;
  readonly method: string;
  readonly path: string;
  readonly params: readonly string[];
  readonly query: readonly string[];
  readonly body: string;
  readonly signed: boolean;
  readonly request: string;
  readonly response: string;
}
{{end}}
{{define "varsOpen"}}{{end}}
{{define "varLine"}}{{end}}
{{define "varsClose"}}{{end}}
{{define "routesOpen"}}
export const routes: readonly Route[] = [
{{end}}
{{define "route"}}  { prefix: {{.Pkg.TSConst}}, service: {{printf "%q" .Service}}, rpc: {{printf "%q" .RPC}}, method: {{printf "%q" .Method}}, path: {{printf "%q" .Path}}, params: [{{join ", " (quoteAll .Params)}}], query: [{{join ", " (quoteAll .Query)}}], body: {{printf "%q" .Body}}, signed: {{.Signed}}, request: {{printf "%q" .Request}}, response: {{printf "%q" .Response}} },
{{end}}
{{define "footer"}}];
{{end}}
