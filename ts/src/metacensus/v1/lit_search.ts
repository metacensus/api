// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: metacensus/v1/lit_search.proto

/* eslint-disable */

export const protobufPackage = "metacensus.v1";

/**
 * Literature search: the PubMed passthrough behind bulk paper import.
 *
 * EXCLUDED ENDPOINT: `POST /lit-search`.
 *
 * It takes `{db, querySearchTerm}`, runs NCBI E-utilities `esearch` followed by
 * `esummary`, and returns the second response body verbatim. That body cannot be
 * expressed as a protobuf message, and not for want of trying:
 *
 *   * `result` is a map keyed by PMID whose values are heterogeneous. Every key
 *     maps to an article summary object except `uids`, which maps to an array of
 *     strings. `map<string, PubMedSearchSummary>` cannot hold that, and
 *     `google.protobuf.Struct` would describe it only by giving up on types
 *     entirely.
 *   * The article summary is NCBI's shape, not MetaCensus's. It has forty-odd
 *     fields with names like `sorttitle`, `nlmuniqueid` and `doccontriblist`,
 *     all lowercase-no-separator. protojson would render a proto field
 *     `sort_title` as `sortTitle`, and the contract does not override field
 *     naming — so every one of those fields would come out misspelled relative
 *     to what NCBI sends and what `types/PubMed.ts` declares.
 *   * It is not MetaCensus data. The response is whatever NCBI returns today; a
 *     change at NCBI is not a change to this contract, and pinning their schema
 *     into ours would make it look like one.
 *
 * The right fix is for the backend to project NCBI's summaries into a
 * MetaCensus shape — the SPA reads about eight fields of the forty — at which
 * point it becomes contractable. Until then, this endpoint stays out and
 * `types/PubMed.ts` remains the client's own business.
 */

/**
 * LitSearchAbstractRequest is the path and query parameters of
 * `GET /lit-search/{id}/abstract?db=`.
 *
 * `id` is a PubMed uid. `db` is the NCBI database name; the SPA always sends
 * `pubmed` and demo defaults to it.
 */
export interface LitSearchAbstractRequest {
  id: string;
  db: string;
}

/**
 * LitSearchAbstract is the response to `GET /lit-search/{id}/abstract`: the
 * abstract of one article, one entry per `<AbstractText>` element.
 *
 * Unlike `POST /lit-search` this one *is* contractable, because demo already
 * projects NCBI's XML down to a list of strings rather than passing a foreign
 * document through.
 *
 * SOURCE CONFLICT: demo returns a bare JSON array, which every response here
 * must not be, so the contract wraps it. It is wrapped as a single resource
 * with a named field rather than as a list with `items`, because these are the
 * paragraphs of one abstract and not a collection of anything.
 *
 * SOURCE CONFLICT: demo maps over DOM nodes with `abstract.textContent`, which
 * is nullable, so the array it returns is typed `(string | null)[]` and can
 * contain nulls for empty elements. A repeated string field cannot hold a null;
 * a backend implementing this contract should drop empty paragraphs instead.
 */
export interface LitSearchAbstract {
  paragraphs: string[];
}
