// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: metacensus/public/v1/common.proto

/* eslint-disable */

export const protobufPackage = "metacensus.public.v1";

/**
 * Shared types for the public surface.
 *
 * `metacensus.public.v1` is a package of its own, not a corner of
 * `metacensus.v1`. The two surfaces differ in caller, in threat model and —
 * the one that decides it — in what compatibility they owe. See README.md,
 * "The two surfaces".
 */

/**
 * Failure is the body of every non-2xx answer on this surface, on every route
 * and on paths no route claims (404, 405). `ok` mirrors the success envelope
 * and is always false.
 *
 * **The HTTP status is the machine-readable signal**, and it is deliberately
 * the only one. `error` is one sentence from a fixed vocabulary, for whoever is
 * reading the response with curl; it is never formatted from an error value, so
 * no internal detail can reach a caller, and it is not stable enough to branch
 * on. An earlier revision of the implementation carried a SCREAMING_SNAKE
 * `code` alongside; it was dropped because it was a second, non-standard
 * encoding of what the status already said, and nothing dispatched on it.
 *
 * The statuses this surface answers with, which `google.api.http` has no way to
 * express and which are therefore prose here:
 *
 *   400  the submission was not valid, or the body was not JSON
 *   403  the `Origin` was absent or not allowlisted
 *   413  the body exceeded 16 KiB
 *   429  the per-client rate limit was exceeded; `Retry-After` carries seconds
 *   502  the submission could not be relayed upstream
 *   500  something else went wrong
 *
 * 400 and 403 carry the same sentence on purpose: which of "a field was wrong"
 * and "that Origin is not allowed" applies is not a caller's business.
 */
export interface Failure {
  /** Always false. Present so success and failure share one discriminator. */
  ok: boolean;
  /** A sentence, not a code. Display it or ignore it; do not branch on it. */
  error: string;
}
